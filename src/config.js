// Configuration settings for the WeightFlow application
// Changing the APP_NAME variable here dynamically updates the branding name across all screens and the document browser title.

export const APP_NAME = 'Kepler';
